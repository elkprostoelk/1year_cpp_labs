/*
 12 Поясніть чому еквівалентні наступні оператори:
 a) if (x) x = 0; і if (x! = 0) x = 0;
 b) if (! x) x = 1; і if (x = = 0) x = 1;
*/
#include <iostream>
using namespace std;
int main()
{
	// a) Потому что если х > 0, то в bool его значение = 1, как и х != 0
	// б) Оператор ! меняет значение на противоположное, то есть если х = 0, то !х возвращает 1, как и х == 0
}